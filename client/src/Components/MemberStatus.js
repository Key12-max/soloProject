import React from 'react';

const MemberStatus = (props) => {
    return (
        <div>
            <h1>MemberStatus: monthId is {props.monthId}</h1>
        </div>
    );
}

export default MemberStatus;
